{
  "name": "tenghui-tu-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "build": "node build.mjs",
    "preview": "node server.mjs --dist",
    "test": "node build.mjs && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
